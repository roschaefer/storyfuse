import { alternatingClassNames } from 'interactive-podcast/helpers/alternating-class-names';
import { module, test } from 'qunit';

module('Unit | Helper | alternating class names');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = alternatingClassNames([42]);
  assert.ok(result);
});
